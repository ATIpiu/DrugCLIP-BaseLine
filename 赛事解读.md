# 赛事解读 — DrugCLIP 高通量虚拟筛选优化智能体

> 第四届世界科学智能大赛 · AI4S 智能体 CNS 挑战赛 · 任务1

---

## 一、赛题本质：在做什么？

这道题的本质是：**让 Agent 自动复现并超越 Science 论文《DrugCLIP》的虚拟筛选性能**。

### 1.1 虚拟筛选是什么问题

给定一个蛋白靶点（口袋结构）+ 一个大型候选分子库（数万到数十万个 SMILES），要求输出每个分子"与该靶点结合"的可能性分数，分数越高排名越靠前。

真实药物发现中，实验室只会验证 **top 1%** 的分子（成本太高，不可能全测）。因此评测的不是"谁预测得最准"，而是"谁能把活性分子富集到最靠前的位置"——即 **EF1%（Enrichment Factor @ top 1%）**。

$$EF_{1\%} = \frac{\text{前1\%候选中活性分子比例}}{\text{全库活性分子比例}}$$

如果随机排，EF1% ≈ 1。好的模型应该达到 5~30。

### 1.2 DrugCLIP 的核心思想

传统 docking（AutoDock 等）：对每个分子单独计算结合能，速度慢（每分子数秒到数分钟），无法扩展到百万级分子库。

DrugCLIP 的创新：用**对比学习**把口袋和分子分别编码为向量，相似度 = 分数：

```
pocket_vec = Encoder_pocket(pocket_atoms)
ligand_vec = Encoder_mol(ligand_atoms)
score = dot(pocket_vec, ligand_vec)
```

推理时只需：
1. 编码口袋一次（固定）
2. 批量编码所有分子
3. 向量内积排序 → 秒级完成百万分子排序

训练用 **InfoNCE 对比损失**：让已知结合对（口袋, 配体）的相似度高，与 batch 内随机负样本的相似度低。

### 1.3 为什么要 Agent？

关键评判点：主办方会审查 `result.log`，确认优化是 **Agent 自动完成**的，而不是人工手调一次性提交。如果日志无法证明自主优化，**分数可能被置零**。

因此 Agent 不是加分项，而是**硬性要求**。

---

## 二、Baseline 各阶段做法及原因

### 阶段一：预训练权重加载（为什么从 Base 出发）

**做法**：加载原版 DrugCLIP checkpoint（`train/model/Base/checkpoint_best.pt`），在其基础上微调，而不是从随机初始化开始训练。

**原因**：
- Base 模型在 PubChem + ChEMBL 千万级分子上预训练，已学会分子 3D 结构的通用表示
- 训练数据（THU-ATOM PDBbind）只有 2808 个复合物，从头训练极易过拟合
- 微调只需更新少量参数，收敛快（10~30 epoch 即可），适合赛事时间窗口

**技术细节**：Base 模型架构为 UniMol Transformer（15层/512维/64头），我们自动检测其架构并适配，避免形状不匹配。

### 阶段二：层冻结策略（为什么冻结前 12 层）

**做法**：冻结 encoder 前 12 层 + embedding + GBF 层，只训练最后 3 层 + projection head。

**原因**：
- 前 12 层已学会原子级别的特征（键型、杂化态、邻域环境），几乎不需要更新
- 最后几层学习任务相关的"口袋—配体"对齐语义，需要针对 PDBbind 微调
- 冻结 75% 的参数 → 可用更大学习率、更少数据收敛，减少过拟合
- GBF（Gaussian Basis Function）编码 3D 距离，与预训练数据分布一致，无需修改

**参数量**：冻结 75,730,378 / 可训练 19,575,297，训练效率提升 ~4×。

### 阶段三：数据预处理流程（为什么需要缓存）

**做法**：两级缓存架构。
- Level-1（tokens.db，SQLite）：SMILES → 3D 构象 + tokenization，永久有效
- Level-2（emb pkl）：SMILES → 向量嵌入，按 checkpoint 版本隔离

**原因**：
- benchmark 共 2,092,260 个配体，RDKit 生成 3D 构象约 44 mol/s，全量需 13 小时
- Level-1 缓存消除了重复 RDKit 计算，推理提速 10~100×
- Level-2 缓存避免了重复的模型前向传播，相同 checkpoint 下单任务 0.6s 完成

### 阶段四：推理评分（为什么用内积而非余弦）

**做法**：pocket_vec 和 ligand_vec 均经过 L2-normalize，然后用**点积（内积）**作为相似度分数。

**原因**：
- L2 归一化后内积 = 余弦相似度，范围 [-1, 1]，可跨任务比较
- 点积比余弦快（避免除法），适合大规模检索
- 原版 DrugCLIP 论文就是这样做的，保持一致性确保预训练权重有效迁移

### 阶段五：Agent 自主优化循环

**做法**：LLM（DeepSeek/OpenAI）驱动的调参循环：
1. `train_model` → 训练，记录 per-epoch loss
2. `analyze_loss` → 分析 loss 曲线，识别过拟合/欠拟合
3. `run_evaluation` → 在 PDBbind val set 计算 EF1%/AUROC
4. `tune_hyperparams` → LLM 根据指标建议下一轮超参
5. 循环 N 轮

**原因**：
- 赛题明确要求 Agent 自主优化，日志必须可证明
- LLM 能处理非结构化的 loss 曲线分析（识别震荡、停滞、发散）
- 自动调参避免人工手动试参的时间成本

---

## 三、评测数据集理解

### DUD-E（102 个任务）

- 设计上：decoy（诱饵）分子与活性分子物化性质相似，但拓扑不同 → 模型不能靠分子大小/极性偷懒
- 每任务：1 个 receptor，1 个共晶配体（用于口袋定位）
- 陷阱：DUD-E 的 decoy 有已知的"人工偏差"（analogue bias），模型可能因此虚高

### LIT-PCBA（15 个任务）

- 比 DUD-E 更真实：活性分子来自 PubChem 实测，更少人工偏差
- 难点：每个任务可能有**多个 receptor 结构**，需决定用哪个（或如何融合）
- 活性率更低（<1%），EF1% 方差更大，需要模型泛化能力更强

### 指标重点

`Mean EF1%` = 117 个任务的 EF1% 均值。DUD-E 任务数量多（102/117），对均值贡献更大，优化 DUD-E 性能性价比更高。

---

## 四、提交格式的关键点

```
result.zip
├── result.csv    # task_id, ligand_id, score
└── result.log    # Agent 自主优化日志（核心审查对象）
```

- **result.log 是胜负关键**：主办方会人工审查日志，确认优化路径合理、有迭代证据
- 每个 (task_id, ligand_id) 必须**恰好出现一次**，缺失任务直接扣分
- score 越高排越靠前，可以是负数，无需归一化到 [0,1]

---

## 五、时间分配建议

| 阶段 | 操作 | 预计时间 |
|------|------|----------|
| 环境搭建 | conda + pip + 下载 Base checkpoint | 30 分钟 |
| 快速验证 | fasttest 3 步跑通 | 10 分钟 |
| 预缓存 | build_mol_cache.py 全量 | 3~6 小时（后台） |
| 微调训练 | PDBbind 20 epoch | 1~3 小时 |
| Agent 调参 | 3~5 轮自动迭代 | 1~2 小时 |
| 全量推理 | 117 任务 | 30 分钟（缓存后）|
| 提交 | 检查 result.zip | 5 分钟 |
