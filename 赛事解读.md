# 赛事解读 — DrugCLIP 高通量虚拟筛选优化智能体

> 第四届世界科学智能大赛 · AI4S 智能体 CNS 挑战赛 · 任务1

---

## 一、赛题本质：在做什么？

这道题的本质是：**让 Agent 自动复现并超越 Science 论文《DrugCLIP》的虚拟筛选性能**。

### 1.1 虚拟筛选是什么问题

想象一下药物研发的现实困境：你有一个致病蛋白（靶点），想从几十万个候选分子里找出能和它结合的"钥匙"。实验室不可能把每一个分子都拿去做实验——成本太高了。实际上，研究员只会挑选排名前 **1%** 的分子去验证。

所以这道题要解决的核心问题是：**把真正有活性的分子尽可能排到最前面**。

评测指标叫 **EF1%（Enrichment Factor @ top 1%）**，可以理解为：

$$EF_{1\%} = \frac{\text{前1\%候选中，活性分子占比}}{\text{全库中，活性分子占比}}$$

**举个例子**：假设全库 10 万个分子里有 100 个活性分子（占比 0.1%）。如果模型随机排，前 1%（1000 个）里平均会有 1 个活性分子，EF1% = 1。如果模型很厉害，前 1% 里有 20 个活性分子，EF1% = 20。好的模型通常能达到 5~30。

### 1.2 DrugCLIP 的核心思想

传统方法（AutoDock 等分子对接）是逐个计算每个分子和靶点的结合能，每个分子要算几秒到几分钟，面对百万级分子库完全不现实。

DrugCLIP 的思路更聪明：把口袋和分子都变成"向量"，然后用向量之间的相似度来打分。

```
pocket_vec = Encoder_pocket(口袋原子坐标)
ligand_vec = Encoder_mol(分子原子坐标)
score = dot(pocket_vec, ligand_vec)   ← 内积越大，越可能结合
```

这样做的妙处在于：
1. 口袋只需编码一次，结果缓存起来
2. 几十万个分子批量编码，向量内积可以矩阵运算，GPU 秒级完成

训练时用 **InfoNCE 对比损失**：把已知能结合的（口袋, 配体）对拉近，把随机负样本推远——本质上和 CLIP 图文对齐是同一个思路。

---

## 二、Baseline 各阶段做法及原因

### 阶段一：预训练权重加载（为什么不从头练）

**做法**：加载原版 DrugCLIP checkpoint（`train/model/Base/checkpoint_best.pt`），在其基础上微调，而不是随机初始化重新训练。

**为什么这么做**：
- Base 模型已经在 PubChem + ChEMBL 的千万级分子上预训练过，相当于读过海量"化学教材"，对分子 3D 结构有很强的基础认知
- 我们的训练数据（THU-ATOM PDBbind）只有 2808 个复合物，数据量太少，从头练很容易过拟合——模型只记住了训练集，泛化能力很差
- 微调只需更新少量参数，10~30 个 epoch 就能收敛，非常适合比赛的时间限制

**技术细节**：Base 模型是 UniMol Transformer（15层/512维/64头），代码会自动检测 checkpoint 的架构并适配，不需要手动配置。

### 阶段二：层冻结策略（为什么冻结前 12 层）

**做法**：冻结 encoder 前 12 层 + embedding 层 + GBF 层，只让最后 3 层和 projection head 参与训练。

**为什么这么做**：
- 可以把 Transformer 的层理解为"从浅到深"的特征提取器。前几层认识的是原子级别的基本特征（化学键类型、杂化态等），这些知识高度通用，不需要动
- 越靠近输出的层，学的越是"任务相关"的语义——比如"这个口袋形状适合哪类分子"，这部分需要针对 PDBbind 的真实结合数据重新调教
- 冻结了 75% 的参数（75,730,378 个），可训练的只剩 19,575,297 个。参数少→过拟合风险小→可以用更大的学习率→收敛更快，大约快 4 倍
- GBF（Gaussian Basis Function）负责把原子间距离编码成偏置，这个模块和 3D 坐标的表示方式绑定，预训练已经学好了，动它反而有害

### 阶段三：两级缓存（为什么不每次实时算）

**做法**：两级缓存架构。
- **Level-1**（`tokens.db`，SQLite）：把 SMILES → 3D 构象 + tokenization 的结果存下来，一次生成永久有效
- **Level-2**（emb pkl 文件）：把分子向量嵌入存下来，按 checkpoint 版本隔离，换模型后会自动重建

**为什么这么做**：
- benchmark 共 2,092,260 个配体，用 RDKit 生成 3D 构象的速度大约是 44 mol/s，全量跑下来要 13 个小时
- 有了 Level-1 缓存，第二次推理时跳过 RDKit，速度提升 10~100 倍
- 有了 Level-2 缓存，连模型前向推断都跳过了，同一个 checkpoint 下单个任务只需 0.6 秒

**命中顺序**：Level-2（最快）→ Level-1（快）→ 实时 RDKit（慢）

### 阶段四：推理评分（为什么用内积而非余弦）

**做法**：pocket_vec 和 ligand_vec 都经过 L2 归一化，然后用**点积（内积）**作为相似度分数。

**为什么这么做**：
- L2 归一化之后，内积在数学上等价于余弦相似度，结果落在 [-1, 1] 之间，不同任务之间可以直接比较
- 实际计算时内积比余弦快（省去了分母的除法），对百万级别的检索很友好
- 原版 DrugCLIP 论文就是这样设计的，保持一致性能确保预训练权重被正确使用

### 阶段五：Agent 自主优化循环

Agent 不只是"调调超参"这么简单，它的能力覆盖了从数据到模型架构的全流程自动优化：

**调参循环**（`tuning_agent.py`）：
1. `train_model` — 训练一轮，记录每个 epoch 的 loss
2. `analyze_loss` — 分析 loss 曲线走势（是在震荡？还是已经停滞？有没有过拟合迹象？）
3. `run_evaluation` — 在 PDBbind 验证集上算 EF1% / AUROC
4. `tune_hyperparams` — LLM 根据指标给出下一轮的超参建议（学习率、冻结层数、batch size 等）
5. 循环若干轮

**模型结构修改**（`model_agent.py`）：
- Agent 可以自动修改 projection head 的维度（当前 128，可调到 256/512）
- 替换或新增 loss function（比如把 Triplet weight 从 0.1 调到 0.3，或者加入 AP Loss）
- 每次改动都写成 diff 记录到日志，确保可审查

**数据处理改进**：
- Agent 可以控制多构象采样数量（每个 SMILES 生成多个 3D 构象，取最优分数）
- 动态调整 hard negative 采样策略（从 DUD-E decoy 库中挑出结构相似但无活性的分子，加大训练难度）

**为什么 Agent 是硬性要求**：
- 赛题明确规定优化必须由 Agent 自主完成，主办方会人工审查 `result.log`
- 如果日志里看不出自主迭代的轨迹（没有多轮决策记录、指标变化、调整理由），**分数可能直接清零**
- LLM 天然擅长处理非结构化的 loss 曲线分析，能识别人眼也容易忽略的细微模式

---

## 三、评测数据集理解

### DUD-E（102 个任务）

- 每个任务：1 个蛋白受体 + 若干已知活性分子 + 大量诱饵（decoy）分子
- 诱饵的设计是刻意让你难以分辨的：和活性分子的分子量、极性等物化性质高度相似，但结构不同、没有活性——模型不能靠"分子长什么样"偷懒，必须学会真正的结合语义
- 注意：DUD-E 有一个已知的偏差问题（"analogue bias"），同一靶点的活性分子结构往往很相近，模型可能靠记住某类骨架来刷高分，实际泛化能力未必那么强

### LIT-PCBA（15 个任务）

- 数据来自 PubChem 的真实实验测量结果，比 DUD-E 更贴近实际药物发现场景
- 难点一：每个任务可能有**多个受体结构**（同一蛋白的不同构象），你需要决定用哪个，或者对多个结构的分数取最大/平均
- 难点二：活性分子比例极低（<1%），模型需要在更"嘈杂"的背景里找到真正的活性分子，对泛化能力要求更高

### 指标重点

最终评分是 117 个任务的 `Mean EF1%` 均值。DUD-E 占 102/117，权重更大，优先把 DUD-E 的分数提上来性价比更高。

---

## 四、提交格式的关键点

```
result.zip
├── result.csv    # 每行：task_id, ligand_id, score
└── result.log    # Agent 自主优化的完整日志（核心审查对象）
```

- **result.log 是胜负手**：主办方会看日志确认优化路径是否合理、有没有多轮迭代证据
- 每个 `(task_id, ligand_id)` 组合必须**恰好出现一次**，漏掉任何任务会直接扣分
- `score` 只要是数值就行，可以是负数，不需要归一化到 [0,1]

---

## 五、时间分配建议

| 阶段 | 操作 | 预计时间 |
|------|------|----------|
| 环境搭建 | conda + pip + 下载 Base checkpoint | 30 分钟 |
| 快速验证 | fasttest 3 步跑通 | 10 分钟 |
| 预缓存 | 后台下载 tokens.db 或运行 build_mol_cache.py | 3~6 小时（后台） |
| 微调训练 | PDBbind 20 epoch | 1~3 小时 |
| Agent 调参 | 3~5 轮自动迭代 | 1~2 小时 |
| 全量推理 | 117 任务 | 30 分钟（缓存后） |
| 提交 | 检查 result.zip | 5 分钟 |
